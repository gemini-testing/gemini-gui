<div class="section section_collapsed" data-suite-id="{{suite.id}}">
    <div class="section__title">{{suite.name}}</div>
    <div class="section__body section__body_guided">
        {{#each states}}
            {{> state this}}
        {{/each}}

        {{#each children}}
            {{> suite}}
        {{/each}}
    </div>
</div>
